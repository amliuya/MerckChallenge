friedman
n_samples = 50 train, 50 test, noise = 1

hyperparameters to vary (for now, picked 4 for both for speed)
random forest
n_estimators, min_samples, max_features, max_samples

xgboost
booster (booster), eta (learning_rate), alpha (reg_alpha), lambda (reg_lambda)